const nextConfig = {
  reactStrictMode: false,
  images : {
    domains : ['facebook.com','platform-lookaside.fbsbx.com', 'localhost:3000', 'picsum.photos'] // <== Domain name
  }
}

module.exports = nextConfig